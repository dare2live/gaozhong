# Python
__pycache__/
*.pyc
*.pyo

# Database (runtime generated)
data/db/
data/db/*.duckdb
data/db/*.duckdb.wal

# Local tooling/runtime artifacts
.codegraph/
logs/

# Large PDFs (local only, use manifest for lineage)
data/textbooks/**/*.pdf
data/curriculum/**/*.pdf
data/junior_high/**/*.pdf
data/external/gaokao_2023_xgkii_english.pdf

# OS
.DS_Store

# 架构审计生成工件 (一次性产物, 不入仓; stop_gate 写 /tmp)
data/reports/project_architecture_audit_*.json

# preview 工具本地配置
.claude/launch.json

# Listening audio candidates (large binaries; sha256 locked in manifests + sources.yaml)
data/external/exam_sources/listening_candidates/*.mp3
data/external/exam_sources/listening_candidates/*.zip
data/external/exam_sources/listening_candidates/*.rar
data/external/exam_sources/listening_candidates/*_extracted/
data/external/exam_sources/listening_candidates/*_extracted/**
