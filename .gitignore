.venv/
__pycache__/
*.pyc
.DS_Store
data/textbooks/_raw_clone/

# start.command runtime logs
logs/

# Sub-repos cloned under data/structured/ — keep the files, drop their .git metadata
data/structured/*/.git/

# DuckDB runtime artifacts (rebuilt by scripts/init_db.py)
data/db/*.duckdb
data/db/*.duckdb.wal

# codegraph local index (regenerated by `codegraph sync`)
.codegraph/
