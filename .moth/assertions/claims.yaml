kind: assertion_pack
name: gaozhong-claims
# 声称-实况对账弹仓 (claims vs reality) — 2026-06-15 创立
# 规则: 任何带数字/状态的关键真相 (本 session 数据诚实性整改成果), 在这里挂一条可执行断言;
# 真相变化时同 commit 更新断言 — 弹仓漂移(红) = 回退/腐烂警报。
# 全部只读; duckdb 连接由 moth 引擎强制 read_only。DB 需先 init_db 建好。
assertions:
  # ===== 真题 provenance 诚实性 (L-N/L-P/L-R) =====
  - id: nonII-not-faking-liaoning
    claim: "非新课标II卷(I/III/甲/乙)不冒充辽宁 (category-aware; 辽宁只用新课标II; L-N/L-P/L-R)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE province LIKE '辽宁%' AND paper_type IN ('新课标 I 卷','新课标 III 卷','全国甲卷','全国乙卷')"
    expect: {op: "==", value: 0}
  - id: liaoning-is-xgkii
    claim: "辽宁卷 paper_type 必为新课标II卷 (辽宁 2015 起只用新课标II)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE province LIKE '辽宁%' AND paper_type <> '新课标 II 卷'"
    expect: {op: "==", value: 0}
  - id: pre2015-not-liaoning
    claim: "2010-2014 辽宁自主命题期不冒充辽宁 (gb 数据为他省新课标II)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE year <= 2014 AND province LIKE '辽宁%'"
    expect: {op: "==", value: 0}
  - id: smoking-gun-gone
    claim: "全国甲卷 'Landscape Photographer' 行不再标辽宁 (L-P smoking gun; 已随 GAOKAO 占位删除)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE raw_question LIKE '%Landscape%Photographer%' AND province LIKE '辽宁%'"
    expect: {op: "==", value: 0}
  - id: eol-truth-imported
    claim: "EOL 真题 2021/2022 辽宁新高考全国II卷已入库 110 题 (M0 真值补全, 替换 GAOKAO 占位)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE source_repo LIKE 'eol_xgkii%'"
    expect: {op: "==", value: 110}
  - id: eol-no-hard-truncation
    claim: "EOL raw_question 无 900 硬截断 (#8: snippet 去 compact 900 限, 阅读篇章全保留; len==900=smoking gun)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE source_repo LIKE 'eol_xgkii%' AND LENGTH(raw_question)=900"
    expect: {op: "==", value: 0}
  - id: local-pdf-reproducible
    claim: "2024/2025 真题 PDF 18 题入库 (M6: init_db Layer 4g 改 in-process import_pdfs(con) 后可复现; 防 subprocess 第二写连接锁冲突回归 → local_pdf=0)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE source_repo = 'local_pdf'"
    expect: {op: "==", value: 18}

  # ===== init_db 全量重建鲁棒性 (M6: git 非 ASCII 路径 + file_manifest) =====
  - id: manifest-nonascii-ok
    claim: "中文名文件正确入 file_manifest (M6: load.py 用 git ls-files -z; 防 quotepath 八进制引号致 init_db 崩回归)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM file_manifest WHERE rel_path LIKE '%上海市初中%'"
    expect: {op: "==", value: 1}

  # ===== 学情派生诚实性 (无假推) =====
  - id: weakness-derived-no-orphan
    claim: "student_weakness 全有 student_answers 在该真考点上的错题支撑 (2026-06-16改: 弱点维度=exam_point真考点非word token; 0 orphan)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM student_weakness w WHERE NOT EXISTS (SELECT 1 FROM student_answers sa JOIN question_bank qb ON qb.qb_id=sa.question_id::BIGINT JOIN edges e ON e.src_id=('question:'||qb.origin_ref) AND e.relation='tests_exam_point' WHERE sa.student_id=w.student_id AND e.dst_id=w.concept_id)"
    expect: {op: "==", value: 0}
  - id: weakness-is-exam-point
    claim: "学情薄弱环节维度=exam_point真考点(非word token假象; critic#2修复, 错题→真考点→薄弱)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM student_weakness WHERE concept_id NOT LIKE 'exam_point:%'"
    expect: {op: "==", value: 0}

  # ===== Phase 7 生成层回滚 (仅真题, 无生成范文) =====
  - id: qbank-real-only
    claim: "question_bank 仅真题, 无合成/生成练习 (Phase 7 回滚; 回退=生成层复活)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM question_bank WHERE origin <> 'real'"
    expect: {op: "==", value: 0}
  - id: course-handouts-empty
    claim: "course_handouts 空 (讲义生成范文已回滚; 教材基石完善前不重建)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM course_handouts"
    expect: {op: "==", value: 0}
  - id: enriched-content-deleted
    claim: "backend/config/enriched_content/ 生成讲义 yaml 已删 (Phase 7 回滚)"
    type: shell
    command: ["bash", "-c", "ls backend/config/enriched_content/*.yaml 2>/dev/null | wc -l | tr -d ' '"]
    expect: {op: "==", value: "0"}

  # ===== 停用词污染清洗 (L-U) =====
  - id: no-stopword-tags
    claim: "question_tags 无功能词 tag (the/it/to/they; L-U 停用词清洗)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM question_tags WHERE tag_id IN ('word:the','word:it','word:to','word:they','word:of','word:a','word:is','word:and')"
    expect: {op: "==", value: 0}
  - id: stopwords-config-present
    claim: "停用词表数据化在 config/stopwords.yaml (§3.5 规则不 hardcode)"
    type: shell
    command: ["grep", "-c", "english_stopwords", "backend/config/stopwords.yaml"]
    expect: {op: ">=", value: 1}

  # ===== 分析诚实 (件1: D0 从"数据诚实"扩展到"分析诚实") =====
  - id: trend-province-scoped
    claim: "命题趋势默认锚定辽宁卷 + 样本量护栏 (件1; 防混284全国卷/薄样本伪装趋势回归)"
    type: shell
    command: ["python3", "-c", "import duckdb;from backend.services import trend;c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);r=trend.question_type_year_trend(c)[0];print('OK' if r.get('province_scope')=='辽宁卷' and 'reliable' in r else 'FAIL')"]
    expect: {op: regex, pattern: "OK"}
  - id: trend-summary-anchored
    claim: "trend_summary 锚定辽宁卷 + 带样本量诊断 (前端首页趋势不混全国卷)"
    type: shell
    command: ["python3", "-c", "import duckdb;from backend.services import trend;c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);s=trend.trend_summary(c);print('OK' if s.get('province_scope')=='辽宁卷' and 'sample_diagnosis' in s else 'FAIL')"]
    expect: {op: regex, pattern: "OK"}
  - id: practice-not-prediction
    claim: "练习卷不冒充'预测/押题': 趋势不可信(辽宁样本不足)时退回蓝图固定占比, 不按 slope 加权 (REVISE predicted.py)"
    type: shell
    command: ["python3", "-c", "import duckdb;from backend.services.exercise.predicted import generate_blueprint_practice_paper as g;c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);p=g(c,total=10);b=p['composition_basis']['selection_basis'];print('OK' if 'blueprint_fixed' in b and p['paper_type']=='blueprint_structured_practice' else 'FAIL')"]
    expect: {op: regex, pattern: "OK"}

  # ===== 词×真题考过状态 单一计算点 (#12/#13/#14: exam_vocab 唯一 tokenizer + exam_coverage 唯一 writer) =====
  - id: exam-status-province-scoped
    claim: "词 exam_status 辽宁口径 (§7, #13): 无 core/HV_extra 节点而辽宁命中(gaokao_hit_count_ln)=0 — 不混外省冒充辽宁考过"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes WHERE node_type='word' AND json_extract_string(attrs_json,'exam_status') IN ('core','HV_extra') AND COALESCE(TRY_CAST(json_extract_string(attrs_json,'gaokao_hit_count_ln') AS BIGINT),0)=0"
    expect: {op: "==", value: 0}
  - id: exam-status-3source-consistent
    claim: "3源一致 (#12): node HV_extra 数 == vocab_classification.jsonl『真超纲·辽宁考过』数 (单一计算点收敛, 三处不再各算矛盾)"
    type: shell
    command: ["python3", "-c", "import duckdb,json;c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);nh=c.execute(\"SELECT count(*) FROM nodes WHERE node_type='word' AND json_extract_string(attrs_json,'exam_status')='HV_extra'\").fetchone()[0];jl=sum(1 for L in open('data/structured/vocab_classification.jsonl',encoding='utf-8') if L.strip() and json.loads(L).get('category')=='真超纲·辽宁考过');print('OK' if nh==jl else 'MISMATCH nh=%d jl=%d'%(nh,jl))"]
    expect: {op: regex, pattern: "OK"}
  - id: gaokao-hit-count-ln-retained
    claim: "gaokao_hit_count_ln 在 word 节点留存>0 (#14: exam_coverage 单一 writer 一次写全, 不再被 extracurricular 整段 UPDATE 覆盖)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes WHERE node_type='word' AND json_extract_string(attrs_json,'gaokao_hit_count_ln') IS NOT NULL"
    expect: {op: ">", value: 0}

  # ===== 考点 canonical 维度 (件2 拱心石: 取代 word-token 冒充考点) =====
  - id: exam-point-edges-present
    claim: "考点 canonical 维度已落库: tests_exam_point 边 ≥300 (dual_model标注批次188 labels, B1去重后落177辽宁题节点 genre/theme)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges WHERE relation = 'tests_exam_point'"
    expect: {op: ">=", value: 300}
  - id: exam-point-dual-model-only
    claim: "考点边只入 dual_model_agree (歧义 needs_review 不入 canonical; provenance 诚实, 不冒充人工核验)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges WHERE relation = 'tests_exam_point' AND json_extract_string(evidence_json, '$.provenance') <> 'dual_model_agree'"
    expect: {op: "==", value: 0}
  - id: exam-point-no-orphan-edge
    claim: "考点边 src 必为真实 question 节点 (无悬挂; Rule 3 edges 一等公民)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges e WHERE e.relation='tests_exam_point' AND NOT EXISTS (SELECT 1 FROM nodes n WHERE n.concept_id=e.src_id AND n.node_type='question')"
    expect: {op: "==", value: 0}
  - id: exam-point-theme-l2-official
    claim: "theme 已细化到课标官方10主题群(L2)落库 (taxonomy v2; 回应'太粗', ≥100条 theme_l2 边)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges WHERE relation='tests_exam_point' AND json_extract_string(evidence_json,'$.dimension')='theme_l2'"
    expect: {op: ">=", value: 100}

  # ===== 工程纪律 (Rule 8) =====
  - id: no-god-module
    claim: "无 >400 行 god-module (Rule 8; 2026-06-15 拆 4 个治理 god-module 后 huge=0)"
    type: shell
    command: ["bash", "-c", "find backend scripts -name '*.py' -not -path '*__pycache__*' | xargs wc -l 2>/dev/null | awk '$2!=\"total\" && $1>400' | wc -l | tr -d ' '"]
    expect: {op: "==", value: "0"}

  # ===== 教材基石完整性 (防"零单元"陈旧说法复发) =====
  - id: textbook-units-extracted
    claim: "教材 78 单元全已抽 (2026-06-16 补 renjiao/bixiu_2 UNIT5 MUSIC, regex 漏抓→override优先 77→78)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM units"
    expect: {op: ">=", value: 78}
  - id: unit-vocab-no-orphan
    claim: "unit_vocab_intro 每词条必有 units PK 行 (Canonical First; 坑: bixiu_2 漏抽U5致9词悬挂, 已修)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM unit_vocab_intro v WHERE NOT EXISTS (SELECT 1 FROM units u WHERE u.version_key=v.version_key AND u.volume_key=v.volume_key AND u.unit_number=v.unit_number)"
    expect: {op: "==", value: 0}
  - id: unit-vocab-no-cross-unit-dup
    claim: "词无跨单元重复 (renjiao 331→0 + waiyan 96→0; 单一区段抽取, 字母总表/复习段不砸进某单元污染 §1.2)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM (SELECT version_key, volume_key, word FROM unit_vocab_intro WHERE unit_number>0 GROUP BY 1,2,3 HAVING COUNT(DISTINCT unit_number)>1)"
    expect: {op: "==", value: 0}
  - id: textbook-sections-have-text
    claim: "219 sections 全有 raw_text 正文 (n_chars>50; 2026-06-16 补10 waiyan单元; 2026-06-17 单元边界收口 221→219)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM section_text WHERE n_chars <= 50"
    expect: {op: "==", value: 0}
  - id: section-text-not-truncated
    claim: "section_text raw_text 不截断 — n_chars==LENGTH(raw_text) 全表 (issue #7: 去 [:20000] 硬截断, n_chars 是真值)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM section_text WHERE n_chars <> LENGTH(raw_text)"
    expect: {op: "==", value: 0}
  - id: section-boundary-no-overwide
    claim: "无 section span>25 (issue #9: waiyan 末单元收口到 Appendices 前 / renjiao UNIT头 override 防误检到 workbook)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM sections WHERE (page_end - page_start) > 25"
    expect: {op: "==", value: 0}
  - id: section-text-no-backmatter
    claim: "section_text 无 back-matter 污染 — 书末专属锚点(Communication bank/Appendices/参考答案)不进主文 raw_text (issue #9 收口副产)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM section_text WHERE raw_text ILIKE '%Communication bank%' OR raw_text ILIKE '%Appendices%' OR raw_text LIKE '%参考答案%'"
    expect: {op: "==", value: 0}
  - id: grammar-occurrences-derived
    claim: "语法 per-unit 已建(§1.2): grammar_occurrences ≥15 从教材Grammar段真相源映射课标项(curated非估算, 不命中诚实跳过)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM grammar_occurrences o WHERE o.grammar_item_id IN (SELECT grammar_item_id FROM grammar_items)"
    expect: {op: ">=", value: 15}

  # ===== D0 总门禁 (权威) =====
  - id: d0-accuracy-green
    claim: "scripts/data_accuracy_check.py 全绿 (D0 100% 准确率)"
    type: shell
    command: ["python3", "scripts/data_accuracy_check.py"]
    expect: {op: regex, pattern: "D0 100% 准确率达成"}
  - id: gaozhong-self-contained
    claim: "互不干扰独立项目: 运行时不读姊妹项目 gaokao 文件路径 (GAOKAO-Bench+真值基线已全镜像本地)"
    type: shell
    command: ["bash", "-c", "python3 -c \"import sys; sys.path.insert(0,'.'); from backend.services.data_sources.extract import gaokao_bench as g; from scripts.tools.audit import truth_baseline_common as t; ok=('Documents/M/gaokao' not in str(g.GAOKAO_DATA)) and ('Documents/M/gaokao' not in str(t.STRUCTURE_PATH)); print('SELF_CONTAINED' if ok else 'DIRTY')\""]
    expect: {op: regex, pattern: "SELF_CONTAINED"}

  # ===== 件3 趋势/考点分布/关联性 (materialize=none, 服务即时算 + D0锁) =====
  - id: exam-point-cooccur-substrate
    claim: "关联性第三条腿底料: 辽宁 ≥100 题挂≥2考点 (跨轴共现源; 件3)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM (SELECT q.question_id FROM exam_questions q JOIN edges e ON 'question:'||q.question_id=e.src_id AND e.relation='tests_exam_point' WHERE q.province LIKE '辽宁%' GROUP BY 1 HAVING count(DISTINCT e.dst_id)>=2)"
    expect: {op: ">=", value: 100}
  - id: trend-distribution-not-materialized
    claim: "趋势/分布/关联性 不落表 (materialize=none 决策; 防快照表悄悄回归致 staleness §3.5)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM information_schema.tables WHERE lower(table_name) LIKE '%exam_trend%' OR lower(table_name) LIKE '%distribution_snapshot%' OR lower(table_name) LIKE '%cooccur%'"
    expect: {op: "==", value: 0}
  - id: heatmap-route-no-inline-agg
    claim: "heatmap 路由无内联 GROUP BY (聚合下沉 services/heatmap, Rule1; 件3)"
    type: shell
    command: ["bash", "-c", "grep 'GROUP BY' backend/api/routes/heatmap.py | wc -l | tr -d ' '"]
    expect: {op: "==", value: "0"}

  # ===== 备课整合 (lesson_plan 一体视图; 词∩真题单一计算点 + 浮窗收口) =====
  - id: lesson-plan-vocab-single-source
    claim: "备课整合词∩真题为单一计算点: recommend/lesson_plan 运行时与 services.vocab 字节一致 (无双算分叉, Rule1)"
    type: shell
    command: ["bash", "-c", "python3 -c \"import duckdb,json; from backend.services import recommend,vocab,lesson_plan as L; c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True); u='unit:waiyan/bixiu_1/U1'; f=lambda o:json.dumps(o,sort_keys=True,ensure_ascii=False,default=str); a=f(recommend.unit_exam_alignment(c,u)); b=f(vocab.unit_word_exam_alignment(c,u)); d=f(L.generate_lesson_plan(c,u)['alignment_summary']); print('OK' if a==b==d else 'DRIFT')\""]
    expect: {op: regex, pattern: "OK"}
  - id: graph-popup-related-via-service
    claim: "讲课浮窗 1-hop related 走 services.graph.neighbors (Rule3 图遍历收口, _fetch_related 不再内联 edges↔nodes JOIN)"
    type: shell
    command: ["bash", "-c", "grep -q 'graph.neighbors' backend/api/routes/graph_popup.py && echo VIA_SERVICE || echo INLINE"]
    expect: {op: regex, pattern: "VIA_SERVICE"}
  - id: lesson-plan-grammar-axis
    claim: "备课整合接入语法轴: generate_lesson_plan 含 grammar 字段且 grammar_occurrences→grammar_items 0 悬挂"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM grammar_occurrences g LEFT JOIN grammar_items gi ON gi.grammar_item_id=g.grammar_item_id WHERE gi.grammar_item_id IS NULL"
    expect: {op: "==", value: 0}
  - id: lesson-plan-grammar-province-scoped
    claim: "备课语法轴真题溯源锚定辽宁卷 (§7; _grammar_exam_trace 带 province LIKE 辽宁%, 不混外省冒充辽宁备课锚)"
    type: shell
    command: ["bash", "-c", "grep -A12 'def _grammar_exam_trace' backend/services/lesson_plan.py | grep -q \"province LIKE '辽宁\" && echo SCOPED || echo LEAK"]
    expect: {op: regex, pattern: "SCOPED"}
  - id: exam-point-theme-l3-substrate
    claim: "颗粒度对齐课标第三级: 辽宁真题挂 theme_l3 子主题边 ≥80 (dual_model opus+sonnet 一致, 诚实跳过分歧)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges e JOIN exam_questions q ON 'question:'||q.question_id=e.src_id WHERE e.relation='tests_exam_point' AND e.dst_id LIKE 'exam_point:theme_l3:%' AND q.province LIKE '辽宁%'"
    expect: {op: ">=", value: 80}
  - id: exam-point-theme-l3-bridged
    claim: "theme_l3 节点全桥到教材 theme (4路追溯不断缝; 0 未桥)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes n WHERE n.concept_id LIKE 'exam_point:theme_l3:%' AND NOT EXISTS (SELECT 1 FROM edges e WHERE e.src_id=n.concept_id AND e.relation='theme_aligns')"
    expect: {op: "==", value: 0}
  - id: graph-popup-questions-liaoning-only
    claim: "讲课浮窗真题 facet 只来自 qbank(157辽宁, §7); _fetch_questions INNER JOIN question_bank 不漏外省空行"
    type: shell
    command: ["bash", "-c", "grep -A8 'def _fetch_questions' backend/api/routes/graph_popup.py | grep -q 'INNER JOIN question_bank' && echo SCOPED || echo LEAK"]
    expect: {op: regex, pattern: "SCOPED"}
  - id: vocab-classification-layered
    claim: "超纲词分层 artifact 就位: ≥1000词分类, 含真超纲·辽宁考过层(词形归并+派生+高考核对+专名过滤, 非精确set差)"
    type: shell
    command: ["bash", "-c", "python3 -c \"import json; rows=[json.loads(l) for l in open('data/structured/vocab_classification.jsonl',encoding='utf-8') if l.strip()]; cats=set(r['category'] for r in rows); print('OK' if len(rows)>=1000 and '真超纲·辽宁考过' in cats and '课标屈折变形' in cats else 'BAD')\""]
    expect: {op: regex, pattern: "OK"}
  # ===== K12 inc1: 中考入单库 + 视图隔离 (exam_type 判别维) =====
  - id: zhongkao-in-single-db
    claim: "中考真题入单库 exam_questions_all (exam_type=中考 90题=2024×45+2025×45; 单库非双库, K12设计§4)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions_all WHERE exam_type='中考'"
    expect: {op: "==", value: 90}
  - id: gaokao-view-excludes-zhongkao
    claim: "高考视图 exam_questions 隔离不含中考 (视图过滤 exam_type=高考; 25+消费者零回归)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE question_id LIKE 'ZK-%'"
    expect: {op: "==", value: 0}
  - id: junior-grammar-namespaced
    claim: "初中语法节点 grammar:jr: 命名空间(71项)不碰高中 grammar; inc2 单库 node_type/stage"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes WHERE concept_id LIKE 'grammar:jr:%'"
    expect: {op: "==", value: 71}
  - id: junior-nodes-no-orphan
    claim: "初中独有 word(~112)+grammar(71) 节点经 at_stage 边连 stage 节点, 非孤儿(orphan门绿)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes n WHERE (n.concept_id LIKE 'grammar:jr:%' OR (n.node_type='word' AND n.attrs_json LIKE '%junior_curriculum%')) AND NOT EXISTS (SELECT 1 FROM edges e WHERE e.src_id=n.concept_id OR e.dst_id=n.concept_id)"
    expect: {op: "==", value: 0}
  - id: k12-deepens-blueprint
    claim: "inc3 跨阶段 deepens: 全71初中语法点都有衔接边(精确59+别名12, 审计HIGH#7补时态/非谓语/定从); 无衔接孤儿"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM nodes WHERE concept_id LIKE 'grammar:jr:%' AND NOT EXISTS (SELECT 1 FROM edges e WHERE e.relation='deepens' AND e.src_id=nodes.concept_id)"
    expect: {op: "==", value: 0}
  - id: k12-stage-materialized
    claim: "inc3 stage 维 materialize: at_stage 边覆盖初中+高中词到5阶段节点(小学/初中/义务教育/高中必修/选修)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM edges WHERE relation='at_stage'"
    expect: {op: ">=", value: 2000}
  - id: multitenant-classes-owned
    claim: "inc6 域B 多租户: demo 班级全有 teacher_id 归属(老师拥有班级链, 隔离前提; 0个无主班)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM classes WHERE teacher_id IS NULL"
    expect: {op: "==", value: 0}
  - id: echarts-vendored-no-cdn
    claim: "ECharts 本地 vendoring 无 CDN 供应链风险 (审计MEDIUM): app.html 不引 cdnjs + 本地文件存在"
    type: shell
    command: ["bash", "-c", "test -f frontend/static/vendor/echarts.min.js && ! grep -q 'cdnjs.cloudflare.com' frontend/app.html && echo OK || echo FAIL"]
    expect: {op: regex, pattern: "OK"}
  - id: zhongkao-jsonl-db-answer-consistent
    claim: "中考答案 JSONL源↔DB 一致 (loader 无漂移; 55题答案逐一对账, 审计MEDIUM 跨年异构兼容)"
    type: shell
    command: ["python3", "-c", "import duckdb,json,glob;rows=[json.loads(l) for p in glob.glob('data/junior_high/exams/*_liaoning/exam_questions.jsonl') for l in open(p,encoding='utf-8')];j={r['question_id']:str(r['answer']).strip() for r in rows if r.get('answer') and str(r['answer']).strip()};c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);d={q:str(a).strip() for q,a in c.execute(\"SELECT question_id,answer FROM exam_questions_all WHERE exam_type='中考' AND answer IS NOT NULL AND TRIM(answer)<>''\").fetchall()};c.close();print('OK' if j==d and j else 'MISMATCH')"]
    expect: {op: regex, pattern: "OK"}
  - id: zhongkao-content-status-honest
    claim: "中考空心诚实标记: content_status 显式标题面/答案完整性, 无'complete'冒充空心 (2024全walled/2025答案待补, 审计HIGH#8)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM zhongkao_questions WHERE content_status IS NULL OR (content_status='complete' AND (raw_question LIKE 'walled%' OR answer IS NULL OR TRIM(answer)=''))"
    expect: {op: "==", value: 0}
  - id: gaokao-count-baseline
    claim: "高考真题计数基线 466 (B1双源去重后, 非陈旧472); 增减须显式改基线防静默漂移 (审计 HIGH#3)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions"
    expect: {op: "==", value: 466}
  - id: gaokao-liaoning-baseline
    claim: "高考辽宁卷计数基线 182 (新课标II §7, B1去重后非陈旧188); 防漂移 (审计 HIGH#3)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions WHERE province LIKE '辽宁%'"
    expect: {op: "==", value: 182}
  - id: zhongkao-answer-fidelity
    claim: "中考MCQ答案保真: 四选一/完形∈{A-D} 五选四∈{A-E} (2024 answer-key-driven 答案=核心交付; 防小写/越界污染, 审计 HIGH#1)"
    type: duckdb_sql
    database: data/db/gaozhong.duckdb
    query: "SELECT count(*) FROM exam_questions_all WHERE exam_type='中考' AND ((question_type IN ('完形填空','阅读理解(四选一)') AND answer IS NOT NULL AND TRIM(answer)<>'' AND TRIM(answer) NOT IN ('A','B','C','D')) OR (question_type LIKE '阅读理解(五选四%' AND answer IS NOT NULL AND TRIM(answer)<>'' AND TRIM(answer) NOT IN ('A','B','C','D','E')))"
    expect: {op: "==", value: 0}
  - id: multitenant-isolation-enforced
    claim: "审计BLOCK修: 跨租户读学生被路由拒(老师A取老师B学生→forbidden); 行为级非仅结构(坑21契约接执行点)"
    type: shell
    command: ["python3", "-c", "import duckdb;from backend.api.routes import students as st;c=duckdb.connect('data/db/gaozhong.duckdb',read_only=True);p=c.execute(\"SELECT a.teacher_id,b.sid FROM (SELECT DISTINCT teacher_id FROM classes WHERE teacher_id IS NOT NULL) a,(SELECT s.student_id sid,cl.teacher_id o FROM students s JOIN classes cl ON cl.class_id=s.class_id) b WHERE a.teacher_id<>b.o LIMIT 1\").fetchone();c.close();r=st.api_students_get({'id':[p[1]],'teacher_id':[p[0]]});print('OK' if 'forbidden' in str(r.get('error','')) else 'LEAK')"]
    expect: {op: regex, pattern: "OK"}
